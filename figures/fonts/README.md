# Fonts

These are the Linux Libertine O OpenType fonts used to render the paper's
vector figures so they match the body text. They are redistributed here under
the SIL Open Font License (OFL), which permits bundling. If they are absent,
the figure scripts fall back to matplotlib's default font automatically.
